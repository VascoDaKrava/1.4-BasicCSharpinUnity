1. Как в одном классе явно реализовать интерфейс и затем в этом же классе метод из реализованного явно интерфейса использовать при реализации другого интерфейса?
Сделал так:
void IFlyable.Fly(Transform transform, float maxHeightFly, float speedFly)
{
...
}

void IUpdatable.DoItInUpdate()
{
((IFlyable)this).Fly(transform, _maxFlyHeight, _flySpeed);
}

Правильно?


2. Какие проблемы будут при явной реализации интерфейса? Звучало, что не получится скастить к нему, но код работает (класс Links):

foreach (Pickup item in GameObject.FindObjectsOfType<Pickup>())
{
item.EventStorageLink = EventStorageLink;
Pickups.Add((IUpdatable)item);
}


3. Явно реализовывать интерфейсы нужно ТОЛЬКО в случаях совпадения названий методов?

4. Глобальный вопрос. Как работаетсистема событий? Вызывается раз в кадр? Кто и как проверяет, сработало ли событие? Насколько это ресурсозатратно?

5. Что значит развернуть обращение к Linq.OrderBy с использованием делегата